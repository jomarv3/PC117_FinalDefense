// Copy this file to Frontend/env.js and adjust it for your machine.
// Use 127.0.0.1 when the browser and backend run on the same computer.
// Use your LAN IP or tunnel URL when another device needs to reach the backend.
window.LIBRARY_CONFIG = {
    API_URL: "http://127.0.0.1:8000/api"
};
